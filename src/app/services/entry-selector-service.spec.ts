import { EntrySelectorService } from './entry-selector-service';

describe('GraphicsService', () => {
  it('should create an instance', () => {
    expect(new EntrySelectorService()).toBeTruthy();
  });
});
