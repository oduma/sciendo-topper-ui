import { OverallSituationService } from './overall-situation-service';

describe('OverallSituationService', () => {
  it('should create an instance', () => {
    expect(new OverallSituationService()).toBeTruthy();
  });
});
